services:
  postgres:
    image: postgres:16-alpine
    container_name: paperprobe-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-paperprobe}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-paperprobe}
      POSTGRES_DB: ${POSTGRES_DATABASE:-paperprobe}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
